let a = 12, b = 4, op = "*";
switch(op) {
  case "+": console.log(a + b); break;
  case "-": console.log(a - b); break;
  case "*": console.log(a * b); break;
  case "/": console.log(a / b); break;
  default: console.log("Invalid operator");
}
// This code performs a basic arithmetic operation based on the operator provided and prints the result to the console.