let n = 5;
for(let i = 1; i <= 10; i++) {
  console.log(`${n} x ${i} = ${n * i}`);
}
// This code generates the multiplication table for the number n (5 in this case) from 1 to 10 and prints each line in the format "n x i = result".