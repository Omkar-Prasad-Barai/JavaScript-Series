let sentence = "JavaScript is awesome";
let count = sentence.trim().split(/\s+/).length;
console.log("Words:", count);
// This code counts the number of words in a sentence by trimming whitespace, splitting the string by spaces, and counting the resulting array's length. The word count is printed to the console.