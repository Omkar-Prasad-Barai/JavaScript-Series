let str = "javascript";
let rev = str.split("").reverse().join("");
console.log("Reversed:", rev);
// This code reverses a string by splitting it into an array of characters, reversing the array, and then joining it back into a string. The reversed string is printed to the console.